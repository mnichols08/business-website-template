# Small Static Site — Setup Guide (Single File)
_Last updated: YYYY-MM-DD_

This one file gets you from zero → running → deployed. Optional tooling (Node/Vite) is included but not required.

---

## 1) Project Overview
**Name:**  
**One-liner:**  
**Live URL (after deploy):**  

**Tech profile (pick what applies):**
- Pure static (HTML/CSS/JS) — _no build_
- Optional tooling: Vite (dev server & bundling)
- Hosting: GitHub Pages (default), Netlify, Cloudflare Pages

---

## 2) Quick Start

### Option A — Pure Static (no build) ✅
```bash
# Clone
git clone https://github.com/<org>/<repo>.git
cd <repo>

# Serve locally (choose one)
# Python 3:
python -m http.server 5173
# OR VS Code "Live Server" extension
```
